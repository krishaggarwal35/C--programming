#include<stdio.h>

int main() {
    int decimal;
    int ans;
    printf("enter the decimal number: ");
    scanf("\n%d",&decimal);
    for (int i=0;decimal!=0;i++)
    {
         ans =decimal%2;
         decimal=decimal/2;
       printf("\n%d",ans);
    }
    return 0;
}
